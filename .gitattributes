# 统一换行符策略：文本文件在仓库中统一为 LF，检出时按平台处理
* text=auto eol=lf

# Windows 批处理脚本保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制文件（示例，后续按需补充）
*.png binary
*.jpg binary
*.ico binary
*.zip binary
*.gz  binary
